@model IEnumerable<PrinterTonerEPCwithAuthentication.Models.ComplaintToner>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Toner.TonerModel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ComplaintTonerDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Remark)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Created)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Toner.TonerModel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ComplaintTonerDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Remark)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Created)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ComplaintTonerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ComplaintTonerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ComplaintTonerID })
        </td>
    </tr>
}

</table>
